module Formatter.Strings exposing (countNewlines)


{-|
Counts the number of newlines in the string

-}
countNewlines : String -> Int
countNewlines string =
    String.foldl
        (\char count ->
            if char == '\n' then
                count + 1

            else
                count
        )
        0
        string

module TypeAlias exposing (Config, Named, Point)


-- Simple record type alias
type alias Point =
    { x : Int
    , y : Int
    }


-- Type alias with a type variable
type alias Config a =
    { name : String
    , value : a
    }


-- Fits on one line
type alias Named =
    { first : String, last : String }

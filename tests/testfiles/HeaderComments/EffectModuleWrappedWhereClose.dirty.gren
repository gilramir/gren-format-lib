effect module EffectModuleWrappedWhereClose where { command = MyCmd
                                                 , subscription = MySub
                                                 } exposing (go, MySub, MyCmd) {- past the list, not about `go` -}


type MyCmd msg
    = C msg


type MySub msg
    = S msg


go =
    1

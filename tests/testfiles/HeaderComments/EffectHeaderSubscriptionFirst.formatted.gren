effect module EffectHeaderSubscriptionFirst where { command = MyCmd, subscription = MySub } exposing (..) {- past the header -}

import Task


type MyCmd a
    = MyCmd (Task.Task Never a)


type MySub a
    = MySub a

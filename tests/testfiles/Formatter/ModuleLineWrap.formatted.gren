module ModuleLineWrap exposing (AlphaType, BetaType, DeltaFunction, EpsilonValue, GammaFunction, ZetaValue)


type AlphaType
    = AlphaType


type BetaType
    = BetaType


gammaFunction =
    1


deltaFunction =
    2


epsilonValue =
    3


zetaValue =
    4

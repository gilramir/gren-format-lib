module ImportExposingMultilineComment exposing (..)

import Basics exposing (max)
import Dict
    exposing
        ( Dict
        , empty {- block comment after the exposed name -}
        -- line comment after the exposed name
        )


theValue =
    1

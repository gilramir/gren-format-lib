module ImportExposingMultilineComment exposing (..)

import Dict exposing
    ( Dict
    , empty {- block comment after the exposed name -} -- line comment after the exposed name
    )
import Basics exposing ( max )

theValue =
    1

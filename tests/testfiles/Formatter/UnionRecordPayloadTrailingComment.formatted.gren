module UnionRecordPayloadTrailingComment exposing (..)


type Tree a
    = Leaf
    | Node { value : a
        , left : Tree a
        , right : Tree a
        } {- tree node with three children -}


{-| A type immediately after, to catch comment drift if the bug recurs. -}
type alias Wrapper =
    { inner : Tree Int }

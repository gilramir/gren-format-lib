module LambdaBracketBodyNestedInCall exposing (updateHeaders)


updateHeaders sinkConfig =
    { sinkConfig | headers = Array.map
            (\headerEntry ->
                { headerEntry | headerName = String.toLower headerEntry.headerName {- lowercase header names -}
                }
            )
            sinkConfig.headers }

module ImportIndentedTrailingComment exposing (x)

import Zebra
    -- indented: this note trails Zebra, so the sort carries it along
import Alpha
-- at column 1: this one leads the import BELOW it, so it stays above Quux
import Quux


x : Int
x =
    1

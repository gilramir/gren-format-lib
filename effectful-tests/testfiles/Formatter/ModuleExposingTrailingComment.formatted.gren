module ModuleExposingTrailingComment exposing
    ( alpha
    , beta
    , gamma
    , delta
    , epsilon
    , zeta
    , eta
    , theta
    )

{- trailing the module exposing list, past its close -}

alpha = 1

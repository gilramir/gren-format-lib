module SimpleArithmetic exposing ( double, square, average )

-- Double a number
double : Int -> Int
double n = n * 2

-- Square a number
square : Int -> Int
square n = n * n

{- Compute the average of two floats -}
average : Float -> Float -> Float
average a b = (a + b) / 2.0
